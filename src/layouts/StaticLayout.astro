---
import Layout from "./Layout.astro";
import Header from "../components/Header.astro";
import Footer from "../components/Footer.astro";
export interface Props {
  title: string;
  ogImageUrl?: string;
  noIndex?: boolean;
}

const { title, ogImageUrl, noIndex } = Astro.props;
---

<Layout title={title} ogImageUrl={ogImageUrl} noIndex={noIndex}>
  <Header />
  <div class="static-container">
    <slot />
  </div>
  <Footer />
</Layout>

<style>
  :root {
    --max-main-width: 620px;
  }

  .static-container {
    width: var(--max-main-width);
    margin-inline: auto;
  }

  @media (width <= 620px) {
    .static-container {
      width: 100%;
    }
  }
</style>
